[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "phidynamics"
version = "0.1.0"
dependencies = [
    "torch",
    "numpy",
    "matplotlib",
    "biopython",
    "scipy"
]
